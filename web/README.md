# Pemba's Field Journal

Interactive five-day Robot Everest diary experience. The first milestone runs entirely from mocked expedition data; Vultr, ROS-bag processing, reflection, and generated narration will be connected later.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
